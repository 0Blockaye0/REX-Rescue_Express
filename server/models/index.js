const User = require('./User');
const Dog = require('./Dog');
const Category = require('./Category');
const Application = require('./Application');

module.exports = { User, Dog, Category, Application };
